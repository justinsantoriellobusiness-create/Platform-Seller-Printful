{
  "mcpServers": {
    "railway": {
      "type": "http",
      "url": "https://mcp.railway.com"
    },
    "chrome-devtools": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "EXEC=\"\"; for c in /opt/pw-browsers/chromium /usr/bin/chromium /usr/bin/chromium-browser /usr/bin/google-chrome; do [ -x \"$c\" ] && EXEC=\"--executablePath=$c\" && break; done; exec npx -y chrome-devtools-mcp@latest ${CLAUDE_CODE_REMOTE:+--headless --isolated --chromeArg=--no-sandbox --chromeArg=--disable-setuid-sandbox --chromeArg=--disable-dev-shm-usage --chromeArg=--disable-gpu} $EXEC ${HTTPS_PROXY:+--proxyServer=\"$HTTPS_PROXY\"}"
      ],
      "env": {}
    },
    "chrome-my-browser": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--browserUrl",
        "http://127.0.0.1:9222"
      ],
      "env": {}
    },
    "firecrawl": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "firecrawl-mcp"
      ],
      "env": {
        "FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY}"
      }
    }
  }
}
